version: "3.9"

# Load environment variables from production env file
x-env-file: &env-file
  env_file:
    - ./internal/config/env/.env.production

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: giraffecloud_api
    ports:
      - "${API_PORT:-8080}:8080"
    depends_on:
      - postgres
    restart: unless-stopped
    <<: *env-file
    volumes:
      - ./logs:/app/logs
    networks:
      giraffecloud_net:
        ipv4_address: 172.17.0.2 # Fixed IP for the API container

  postgres:
    image: postgres:16-alpine
    container_name: giraffecloud_postgres
    <<: *env-file
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${DB_NAME}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      giraffecloud_net:
        ipv4_address: 172.17.0.3 # Fixed IP for the postgres container
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  giraffecloud_net:
    name: giraffecloud_net
    ipam:
      config:
        - subnet: 172.17.0.0/16
          gateway: 172.17.0.1

volumes:
  postgres_data:
    driver: local
